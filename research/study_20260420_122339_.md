# 

**研究日期**：2026-04-20 12:23:39
**來源**：https://github.com/Lzxpan/CodeWorker
**標籤**：程式碼助理, Windows, LLM

---

## 📌 關鍵資訊
標題：[...]
來源：[...]
日期：[...]

摘要：CodeWorker V1.01.000 是一款离线且优先考虑隐私和安全的 Windows本地LLM程式码助理。它整合了`llama.cpp`、`WinPython`、`PortableGit`和GGUF模型，并附带一个Web UI。此工具适用于不能上传原始代码、无法使用云端模型，或需要带到客户端、内网或隔绝环境的开发场景。主要功能包括本地模型服务、选型能力（可选择多个模型）、上下文下拉菜单以及多项附件分析技术。

標籤：程式碼助理, Windows, LLM

---

## 📄 原文內容（部分）

```
# CodeWorker V1.01.000

> 離線、可攜、以隱私與資安為優先的 Windows 本地 LLM 程式碼助理。

[繁體中文完整說明](README.zh-TW.md) | [English](README.en.md)

---

## 1. 功能說明

`CodeWorker` 將 `llama.cpp`、`WinPython`、`PortableGit`、GGUF 模型與 Web UI 整合在同一個 Windows 工作目錄。它適合不能上傳原始碼、不能使用雲端模型，或需要帶到客戶端、內網、air-gapped environment 的開發情境。

主要能力：

- 本機模型服務：預設使用 `Gemma 4 26B`，由 bundled `llama.cpp` service 啟動，不需要 Ollama。
- 可選模型：保留 `Qwen 3.5 9B Vision`，兩個模型都走同一套 chat、RAG、附件與安全流程。
- Context 下拉選單：每個模型可獨立選擇 `4k / 8k / 16k / 32k / 64k / 128k / 256k`，預設 `256k`。
- 全專案檢索：開啟專案後，即使沒有 pinned files，也會使用本機 RAG index 搜尋相關檔案、symbols、summary 與 chunks。
- 聚焦上下文：在 `檔案樹` 勾選檔案時，pinned files 會優先於全專案 RAG。
- 附件分析：支援程式碼、設定、文件、圖片、音訊與影片；可抽文字、keyframes 或 transcript 時會送入模型，否則送 metadata fallback。
- 多對話串：右側 `240px` thread panel 可新增、切換、重新命名、刪除對話串，每個 thread 保留自己的 history 與 memory。
- 模型主導檔案生成：在一般對話中要求產生文件即可，模型會先產生內容與標題，CodeWorker 依模型標題自動命名並建立 pending preview；確認後才寫入 `.txt/.md/.py/.js/.ts/.json/.html/.css/.yaml/.sql/.cs/.docx/.pdf/.pptx/.xlsx`。
- Agent 安全機制：寫檔、patch、刪檔與執行 command 前都會建立 pending action，使用者確認後才執行。

---

## 2. 重點注意事項

- 第一次執行需要網路下載 runtime 與模型；完成後可離線使用。
- `256k` context 是可選上限，不代表每台機器都能穩定跑滿；若模型啟動失敗，UI 會顯示錯誤與 log path，不會自動降級。
- 建議至少 `32GB RAM` 等級；大型 context、圖片、影片 keyframes 與長回答都會增加記憶體壓力。
- 未開啟專案時只做一般問答；已開啟專案且未釘選檔案時使用全專案 RAG；有 pinned files 時優先使用 pinned context。
- 影片不是直接把 MP4 binary 丟給模型，而是先用 `FFmpeg` 抽 keyframes；音訊與影片音軌會嘗試 `whisper.cpp` speech-to-text。
- 檔案生成與 Agent 寫入都必須確認後才會落到 project root；生成完成後 UI 會顯示實際檔案路徑與檔名。

---

## 3. 安裝方式

### 第一次完整準備

```cmd
scripts\bootstrap.cmd
```

這會依 `config\bootstrap.manifest.json` 準備：

- `llama.cpp`
- `WinPython`
- `PortableGit`
- `FFmpeg`
- `whisper.cpp` 與 speech-to-text model
- `Gemma 4 26B` / `Qwen 3.5 9B Vision` GGUF 與 `mmproj`
- Python 文件套件：`pypdf`、`pdfplumber`、`python-docx`、`reportlab`、`python-pptx`、`openpyxl`

### 啟動 Web UI

```cmd
scripts\launch-webui.cmd
```

開啟：

```text
http://127.0.0.1:8764
```

### 選用 CLI agent

```cmd
scripts\install-aider.cmd
```

---

## 4. 使用方式與教學

### 畫面範例

![CodeWorker V1.01.000 繁中 Web UI 畫面](docs/screenshots/webui-overview-zh-v101000.png)

### 一般問答

1. 啟動 Web UI。
2. 不必開啟專案，直接在主對話框提問。
3. 此模式不會加入 `PROJECT RAG CONTEXT`、pinned files 或 file tree。

### 專案搜尋與問答

1. 在 `專案路徑` 選擇專案根目錄。
2. 點 `開啟專案`。
3. 需要更新快取時點 `分析專案`。
4. 直接詢問「在哪個檔案」、「哪段 code」、「要怎麼修改」；沒有 pinned files 時會自動用 RAG 搜尋全專案。
5. 若要聚焦少數檔案，在 `檔案樹` 勾選檔名。

推薦問題：

- 「請問加載 model 的 code 在哪個檔案的哪一段？」
- 「想更新遊戲速度要怎麼修改？請列出檔案路徑與原因。」
- 「我加入網路連線對戰，第一步要改哪些檔案？」

### Context 設定

1. 在聊天輸入區底部的 `Context` 下拉選單選擇 `4k` 到 `256k`。
2. 每個模型會記住自己的選擇。
3. 如果現有 `llama-server` context 低於目前選擇，下一次啟動模型時會用新 context 重啟。
4. 若 `256k` 在本機失敗，請看左側錯誤區與 `logs/llama-server-*.err.log`。

### 對話串

- 右側 `對話串` 可新增、切換、重新命名與刪除 thread。
- 每個 thread 保存自己的 `history`、`memory_summary`、`modelKey` 與 `projectPath`。
- `清空對話` 只清目前 thread，不會清掉其他 thread。

### 附件分析

1. 點 `上傳檔案`，或把截圖貼到聊天輸入區。
2. 可上傳程式碼、設定檔、PDF、DOCX、圖片、音訊與影片。
3. 圖片會先嘗試 native vision；影片會先抽 keyframes；音訊會嘗試 speech-to-text。
4. 若 extractor 或 native payload 不可用，CodeWorker 會送 metadata 與限制說明，不會假裝已看見內容。

### 檔案生成

1. 開啟
...
```

---

## 🔬 四層分析

### 第一層：白話解構

#### 這篇文章在講什麼？
文章描述了一個本地化的 LLM 程式碼助理，名为 `CodeWorker` V1.01.000，它可以在离线和受限制的环境中运行，并且优先考虑隐私和安全性。文章详细介绍了它的功能、注意事项以及安装与使用方法。

#### 主要在说什么故事或概念？
文章讲述了一个本地化的 LLM 程式码助理（CodeWorker）的功能特性及其使用方法。主要强调它可以在不连接互联网的情况下工作，并且具有强大的本地模型服务，支持离线编程和文档生成功能。同时，还提到了一些关键注意事项和安全机制。

#### 目标读者是谁？
目标读者是希望在本地或受限制环境中使用 `CodeWorker` 的开发人员以及对该工具的技术细节感兴趣的用户。

### 第二層：技術驗證

#### 這篇文章的 claims 有哪些？
1. `CodeWorker` 是一个离线可用、以隐私和安全性为优先的 Windows 本地 LLM 程序码助理。
2. 它可以使用本地模型服务，也可以选择外部模型。
3. 提供上下文下拉菜单来选择不同大小的上下文。
4. 支持多对话串模式，每个对话串有独立的历史记录和记忆。
5. 具有强大的附件分析能力，能够处理包括文字、图片、音视频在内的多种文件类型。
6. 支持自动生成文档（如报告、PPT等）。
7. 提供了一个详细的上下文配置选项界面。
8. 通过用户交互确认来确保所有操作的安全性。

#### 哪些是有根据的？哪些可能是错的？
- 背后的原理和技术实现（如 LLM 模型服务和附件分析方法）都是有据可循的。
- 文章中提到的上下文大小设置、模型选择以及某些具体的技术细节可能会因为版本更新或其他因素有所不同，因此需要用户自行验证其正确性。

#### 有沒有邏輯漏洞或 bias？
- 在逻辑上，文章内容没有明显的漏洞或偏见。但是，一些具体的配置选项和默认值可能因使用不同的数据集或模型而有所差异，这在实际应用中可能需要进行适当的调整。
- 关于上下文大小的选择问题，虽然文章提到了不同选择的后果，但具体如何设置以及是否适合用户需求可能会根据用户的实际情况有所不同。

### 第三層：核心洞察

#### 這篇文章最重要的三个 insight 是什麼？
1. **CodeWorker 是一个在本地环境中运行的 LLM 程序码助理。**
   - `CodeWorker` 具有强大的本地模型服务功能，可以在不依赖外部资源的情况下进行编程和文档生成。
   
2. **它支持用户自定义上下文大小。**
   - 用户可以通过界面选择不同大小的上下文，以便更精确地控制聊天和文件检索的结果。
  
3. **CodeWorker 提供了多对话串机制以提高灵活性。**
   - 在同一个项目中可以同时处理多个对话串，每个对话串都有自己独立的历史记录和记忆，提高了协作与信息管理的效果。

#### 對讀者最有價值的啟發是什麼？
- CodeWorker 是一个强大且灵活的选择，特别适合那些需要在受限制或离线环境中进行编程和文档生成的开发人员。它提供了一个完整的上下文控制选项来确保程序正确性和安全性。
  
#### 如果只能带走一件事，会是什么？
如果只能带走一件最有价值的事情，那么应该是 `CodeWorker` 的核心特性之一：强大的本地模型服务功能，因为它能够在任何环境下独立运行，并且在某些情况下可能比依赖云环境的解决方案更加安全和高效。

### 第四層：系統整合建議

#### 這個東西怎麼用在我們現有的系統？
要将`CodeWorker`集成到现有的系统中，可以考虑以下几个步骤：

1. **设置一个单独的工作目录。**
   - 在系统中创建一个新的文件夹来存储 `CodeWorker` 的配置文件和其他相关数据。

2. **安装必要的依赖项。**
   - 根据文章中的说明，在 Windows 系统中通过命令行执行脚本来安装 `llama.cpp`、`WinPython` 和其他需要的组件。

3. **准备上下文环境。**
   - 为不同大小的上下文配置不同的模型和文件索引，并确保所有必要的资源都在指定的工作目录下准备好。

4. **运行 WebUI 并启动服务。**
   - 使用文章中提到的脚本来启动前端界面，并通过调用 `launch_llama_server.py` 和其他相关脚本来初始化本地 LLM 模型服务器。

5. **编写和使用代码**。
   - 用户可以通过与 `CodeWorker` 进行交互来输入问题或指令，然后根据需要生成文档或其他形式的输出。同时还可以通过右侧面板管理多对话串，以更好地组织和协调不同的对话记录。

6. **安全性和隐私保护**。
   - 确保所有的数据处理、文件上传和附件分析都符合相关的安全标准，并且适当控制敏感信息的访问权限。

7. **文档与学习资源**。
   - 提供详细的用户手册和其他学习资料，帮助系统管理员或开发者更好地理解和使用 `CodeWorker`。此外，可以考虑在现有系统中整合一个模组或者规则来简化集成过程和减少配置工作量。

---

## 💾 元資料

- **研究時間**：2026-04-20 12:23:39
- **來源 URL**：https://github.com/Lzxpan/CodeWorker
- **處理模型**：qwen2.5:3b (本地 Ollama)

---

*由 EnhancedStudy 技能自動產生*
