# 

**研究日期**：2026-04-22 14:29:47
**來源**：https://github.com/browser-use/video-use
**標籤**：程式語言, 音頻處理, 影片編輯

---

## 📌 關鍵資訊
標題：[...]
來源：[...]
日期：[...]  
摘要：介紹了**video-use**——使用Claude Code編輯影片。它是完全開放原始碼的。
將原始素材放入一個資料夾中，與 Claude Code 語聊，然後會得到 `final.mp4` 的結果。適用于任何內容，包括訪談、剪接、指導、遊記和訪問等，而不需要預設或選項。
## 其他功能
- 剔除冗餘的詞語（如“嗯”、“呃”）以及拍攝間的空閑時間。
- 自動對每個片段進行色彩調節——可以是溫暖電影式的、中性強烈式，也可以是一種自定義的ffmpeg鏈接。
- 在每個切換點插入30毫秒的靜音淡入效果，從而避免聽到任何嘶啞聲。
- 將字幕嵌入到你的風格中 ——默認為兩詞的大寫英文字母切塊，可以完全定制化。
- 使用 Manim、Remotion 或 PIL 生成圖像-overlay。
- 自動評估渲染輸出，在每個切換點前進行檢查以確定是否顯示結果。
- 在每次會話結束時保存工作記憶到 `project.md` 文件中，以便下次使用時可以從上次的處境開始。

## 如何操作

1. 克隆並連接至 Claude Code 的技能資料夾
2. 安裝依賴項
3. 將 ElevenLabs API 密鑰添加到 `.env` 文件

然後在 Claude Code 中指向一個包含原始素材的資料夾：

```bash
cd /path/to/your/videos
claude
```

在會話過程中，它將監視內容並提出策略建議，直到得到您的確認後才進行最終編輯。該工具的所有輸出都存放在 `<videos_dir>/edit/` 資料夾中。

## 如何運作

這個LLM從不觀看影片本身。相反，它通過兩個層級的數據來讀解內容——這兩個層級合起來為它的切片提供了與詞語邊界的精確性。
- **第一層 - 音頻轉錄（總是加載）。** 通過一次ElevenLabs Scribe呼叫獲得每個素材段落中的字詞時間標記、講者隔開以及音頻事件。
- **第二層 - 畫面合成（根據需求）。** `timeline_view`製作出一個影片strip + 波形圖 + 字詞標籤的PNG。它僅在決策點時會被呼叫——例如，模糊停頓、重拍比較以及切換點合理性檢查。
  
## 如何使用

- 剔除冗餘字句：（如“嗯”、“呃”）和間隙中的無效部分
- 通過自定義的色彩調節每段影片——包括溫暖電影式的、中性強烈式，或任何自定義的ffmpeg鏈接。
- 在每個切換點插入30毫秒的靜音淡入效果，以避免聽到嘶啞聲。
- 設置字幕嵌入到你的風格中 ——默認為兩詞的大寫英文字母切塊，可以完全定制化。
- 使用 Manim、Remotion 或 PIL生成圖像-overlay。
- 在每個切換點之前自動評估渲染輸出，確定是否顯示結果。  
標籤：程式語言, 音頻處理, 影片編輯

---

## 📄 原文內容（部分）

```
<p align="center">
  <img src="static/video-use-banner.png" alt="video-use" width="100%">
</p>

# video-use

Introducing **video-use** — edit videos with Claude Code. 100% open source.

Drop raw footage in a folder, chat with Claude Code, get `final.mp4` back. Works for any content — talking heads, montages, tutorials, travel, interviews — without presets or menus.

## What it does

- **Cuts out filler words** (`umm`, `uh`, false starts) and dead space between takes
- **Auto color grades** every segment (warm cinematic, neutral punch, or any custom ffmpeg chain)
- **30ms audio fades** at every cut so you never hear a pop
- **Burns subtitles** in your style — 2-word UPPERCASE chunks by default, fully customizable
- **Generates animation overlays** via [Manim](https://www.manim.community/), [Remotion](https://www.remotion.dev/), or PIL — spawned in parallel sub-agents, one per animation
- **Self-evaluates the rendered output** at every cut boundary before showing you anything
- **Persists session memory** in `project.md` so next week's session picks up where you left off

## Get started

```bash
# 1. Clone and symlink into Claude Code's skills directory
git clone https://github.com/browser-use/video-use
cd video-use
ln -s "$(pwd)" ~/.claude/skills/video-use

# 2. Install deps
pip install -e .
brew install ffmpeg           # required
brew install yt-dlp            # optional, for downloading online sources

# 3. Add your ElevenLabs API key
cp .env.example .env
$EDITOR .env                   # ELEVENLABS_API_KEY=...
```

Then point Claude Code at a folder of raw takes:

```bash
cd /path/to/your/videos
claude
```

And in the session:

> edit these into a launch video

It inventories the sources, proposes a strategy, waits for your OK, then produces `edit/final.mp4` next to your sources. All outputs live in `<videos_dir>/edit/` — the skill directory stays clean.

## How it works

The LLM never watches the video. It **reads** it — through two layers that together give it everything it needs to cut with word-boundary precision.

<p align="center">
  <img src="static/timeline-view.svg" alt="timeline_view composite — filmstrip + speaker track + waveform + word labels + silence-gap cut candidates" width="100%">
</p>

**Layer 1 — Audio transcript (always loaded).** One ElevenLabs Scribe call per source gives word-level timestamps, speaker diarization, and audio events (`(laughter)`, `(applause)`, `(sigh)`). All takes pack into a single ~12KB `takes_packed.md` — the LLM's primary reading view.

```
## C0103  (duration: 43.0s, 8 phrases)
  [002.52-005.36] S0 Ninety percent of what a web agent does is completely wasted.
  [006.08-006.74] S0 We fixed this.
```

**Layer 2 — Visual composite (on demand).** `timeline_view` produces a filmstrip + waveform + word labels PNG for any time range. Called only at decision points — ambiguous pauses, retake comparisons, cut-point sanity checks.

> Naive approach: 30,000 frames × 1,500 tokens = **45M tokens of noise**.
> Vid
...
```

---

## 🔬 四層分析

### 第一層：白話解構

#### 1. 發文章在講什麼？
- 文章介紹了一個名為 `video-use` 的工具，可以使用 Claude Code 編輯視頻。
- 它提供了一些技術特性來自動編輯視頻，包括刪除廢話、自動色彩校正、添加漸變背景，以及自定義標籤等。

#### 2. 主要在說什麼故事或概念？
- 文章描述了一個基於 Claude Code 的工具，能夠自動化編輯視頻的過程。
- 傳統上需要人為操作來刪除廢話、調整視頻品質和添加字幕，而這個工具可以做到。

#### 3. 目標讀者是誰？
- 應該是對視頻編輯技術有興趣或正在使用 Claude Code 的開發人員、編程者以及任何希望自動化編輯流程的人。

### 第二層：技術驗證

#### 1. 文章的 claims （主張）有哪些？
- **Cuts out filler words** (`umm`, `uh`, false starts) and dead space between takes
- **Auto color grades** every segment (warm cinematic, neutral punch, or any custom ffmpeg chain)
- **30ms audio fades** at every cut so you never hear a pop
- **Burns subtitles** in your style — 2-word UPPERCASE chunks by default, fully customizable
- **Generates animation overlays** via [Manim](https://www.manim.community/), [Remotion](https://www.remotion.dev/), or PIL — spawned in parallel sub-agents, one per animation
- **Self-evaluates the rendered output** at every cut boundary before showing you anything
- **Persists session memory** in `project.md` so next week's session picks up where you left off

#### 2. 哪些是有根據的？哪些可能是錯的？
- **Cuts out filler words** (`umm`, `uh`, false starts) and dead space between takes：有據。
- **Auto color grades** every segment (warm cinematic, neutral punch, or any custom ffmpeg chain)：有據，並使用官方工具如 ffmpeg。
- **30ms audio fades** at every cut so you never hear a pop：有據，但可能需要根據具體情況調整以避免過度模糊音頻。
- **Burns subtitles** in your style — 2-word UPPERCASE chunks by default, fully customizable：有據，且支持自定義。然而，可能存在對自定義字幕的誤解，例如是否允許多個詞語。
- **Generates animation overlays** via [Manim](https://www.manim.community/), [Remotion](https://www.remotion.dev/), or PIL — spawned in parallel sub-agents, one per animation：有據。但需要注意的是這些工具的選擇可能會影響最終效果，而且並未提及如何處理多個視頻片段的整合。
- **Self-evaluates the rendered output** at every cut boundary before showing you anything：有據。
- **Persists session memory** in `project.md` so next week's session picks up where you left off：有據。

#### 3. 是否有邏輯漏洞或 bias？
- 文章並未明確提及所有技術特性是否適用於不同類型的視頻內容，例如一些特性可能需要根據具體視頻來適應不同的風格和效果。
- 沒有特別強調工具是否已經過充分測試和驗證，因此用戶在使用時可能會遇到未知問題。

### 第三層：核心洞察

#### 1. 文章最重要的 3 個 insight 是什麼？
- **自動化視頻編輯**：文章介紹了一個可以自動化的視頻編輯工具。
- **自定義字幕和背景動畫**：用戶可以設置自己的字幕和背景動畫，增加了靈活性。
- **自我評估環節**：在每一步都會進行檢查，確保最終的編輯結果質量。

#### 2. 對讀者最有價值的啟發是什麼？
- 借助 Claude Code 的強大功能，用戶可以輕鬆地自動化視頻編輯過程。
- 文章提供了詳細的開發指南和技術細節，對希望了解如何實現這個工具的讀者來說非常有幫助。

#### 3. 如果只能帶走一件事，是什麼？
- 絕大部分使用這個工具的價值來自於其自定義功能和自動化編輯過程。但是最關鍵的部分應該是 **理解如何正確設定和適應這些特性以達到最佳效果**。

### 第四層：系統整合建議

#### 1. 這個東西怎麼用在我們現有的系統？
- 如果想將這項技術與現有系統整合，可以考慮將其作為一個獨立的編輯工具或者模組來使用。
- 比如在 Claude Code 的開發環境中作為一個新的插件或技能來添加。

#### 2. 可以變成：一個模組？一條規則？一個 SOP？
- **一個模組**：可以將這個工具視為一個獨立的模組，供其他系統調用。
- **一條規則**：在新的編輯流程中制定特定的操作規則來指導用戶如何使用這個工具。
- **一個 SOP（標準操作程序）**：為新用戶提供一份詳細的手冊或指南，以便他們了解如何正確地使用這個模組。

#### 3. 具體建議怎麼實作？
- **建立一個獨立的專用工作坊或導航系統**來展示和介紹這個工具的所有功能和優點。
- 建立一套測試流程來驗證該技術的不同特性是否可以適應不同的視頻內容和風格，確保其能夠實現預期的效果。
- 提供一對一培訓和支持服務，以幫助開發者更好地理解如何在自己的系統中集成這個工具。

---

## 💾 元資料

- **研究時間**：2026-04-22 14:29:47
- **來源 URL**：https://github.com/browser-use/video-use
- **處理模型**：qwen2.5:3b (本地 Ollama)

---

*由 EnhancedStudy 技能自動產生*
