# Vault-for-LLM

**研究日期**：2026-04-20 12:33:43
**來源**：https://github.com/zycaskevin/Vault-for-LLM
**標籤**：人工智慧, 知識管理系統, LLM

---

## 📌 關鍵資訊
標題：[Vault-for-LLM]
來源：[未指定]
日期：[未提供]
摘要：Vault-for-LLM是一個本地化的、開放源碼的知識管理系統，專為LLM代理設計。它使用SQLite + sqlite-vec + ONNX嵌入值進行運行，不需要雲端或Docker，只需要通過`pip install`即可安裝。該系統具有四層結構的知識庫，包括身份層（誰在對話中）、核心事實層、上下文層和深度知識層，用於存儲並提供可搜索的長期記憶。它還支持混合查詢（關鍵字 + 语義向量）以及知識圖形，可以自動推斷實體和邊緣關係。
標籤：人工智慧, 知識管理系統, LLM

---

## 📄 原文內容（部分）

```
# Vault-for-LLM

**[繁體中文](README.zh-Hant.md) | [简体中文](README.zh-CN.md) | English**

> 🧠 A local-first, open-source knowledge management system for LLM agents.
> Zero cloud dependency. Zero Docker. Zero PyTorch. Just `pip install` and go.

---

## What is Vault-for-LLM?

Vault-for-LLM is a **four-layer hierarchical knowledge base** designed to give any LLM agent persistent, searchable memory. It runs entirely locally using SQLite + sqlite-vec + ONNX embeddings.

### Key Features

- **Four-layer architecture** (L0–L3) for structured knowledge injection
- **Hybrid search**: keyword + semantic vector search (ONNX, no GPU needed)
- **Knowledge graph**: auto-inferred entities and edges with BFS expansion
- **AAAK compression**: 6x compression for compiled knowledge
- **Trust scoring**: every knowledge entry has a confidence score (0.0–1.0)
- **Lint & contradiction detection**: automatic quality checks
- **CLI-first**: 20+ commands for full lifecycle management

---

## Architecture

```
L0 Identity      → Who the user is (injected every conversation)
L1 Core Facts    → Environment & active projects (injected every conversation)
L2 Context       → Recent decisions & troubleshooting (auto-updated daily)
L3 Deep Knowledge → Architecture, techniques, lessons (searched on demand)
```

---

## Quick Start

```bash
# Install
pip install -e .

# Initialize a project
vault init

# Add knowledge
vault add "My First Entry" --content "Something I learned today"

# Compile (raw → database + compiled)
vault compile

# Search
vault search "my query"

# Health check
vault doctor
```

See [INSTALL.md](INSTALL.md) for detailed installation options.

---

## Directory Structure

```
your-project/
├── vault.yaml          ← Project config (auto-generated by `vault init`)
├── L0-identity/             ← Who the user is (injected every conversation)
│   └── identity.md
├── L1-core-facts/           ← Core facts (injected every conversation)
│   └── current-projects.md
├── L2-context/              ← Dynamic context (auto-updated daily)
│   └── recent-sessions/
│       └── current.md
├── L3-knowledge/            ← Deep knowledge (searched on demand)
├── raw/                     ← Raw knowledge input (your .md files go here)
├── compiled/                ← AAAK compressed backup (auto-generated)
└── templates/               ← Clean templates for L0/L1/L2
```

---

## AI Integration Guide

### Any LLM Agent (Universal)

1. Read this README to understand the architecture
2. Read `L0-identity/identity.md` to know the user
3. Read `L1-core-facts/current-projects.md` for current state
4. Use `vault search "query"` for semantic search

### Claude Code / Cursor / Any AI IDE

1. Copy `CLAUDE.md` (included) into your project root
2. For deep knowledge, search `compiled/` or `raw/`
3. Use `rg "keyword" raw/ compiled/` for fast lookup

---

## CLI Reference

| Command | Description |
|---------|-------------|
| `vault init` | Initialize a new project |
| `vault doctor` | Health check |
| `va
...
```

---

## 🔬 四層分析

## 第一層：白話解構

### 何謂 Vault-for-LLM？

Vault-for-LLM 是一套為 LLM 應用程式的 **分層知識庫**，旨在提供任何 LLM 應用程式持久且可搜尋的記憶體。它使用 SQLite + sqlite-vec + ONNX 嵌入式技術完全運行在本地。

### 主要故事或概念

- Vault-for-LLM 是一套四層分類知識庫，每個層都包含不同的知識。
- 它允許 LLM 應用程式使用 SQLite 簡單地存儲、更新和查詢資料。
- Vault-for-LLM 提供關鍵功能包括：自動編輯知識以進行語義搜索（無需 GPU），預設設定為本地環境。

### 目標讀者

目標讀者是希望構建自己的 LLM 應用程式並管理記憶體的開發者和技術人員。對於任何希望設計一個可以與 LLM 應用程式互動且具有特定知識需求的使用者來說，本指南都是一個很好的參考。

## 第二層：技術驗證

### 認斷聲明

1. **四層分類架構** (L0–L3) 是結構化知識存儲的核心。
2. **混合搜索引擎** 包括詞彙組合與語義向量，可以不用 GPU。
3. 使用 BFS 演算法自動推導實體和邊界。
4. AAAK 压縮是 6:1 的數據壓縮。
5. 知識點之間的信任評分範圍為 0 到 1（最高）。
6. 自動化品質檢查會定期進行語法與矛盾分析。
7. CLI 較為首要的使用方式，包含多個命令。

### 基於上述聲明是否有根據？

- **四層架構** 是一個有根據的概念，因為這確實是知識存儲的核心結構。
- **混合搜索引擎** 使用 ONNX 專利技術進行語義向量查詢，但是否需要 GPU 取決於應用程式需求。然而，在許多情況下，不需要 GPU 也能實現高效率的語義搜尋，因此這個聲明似乎是有根據的。
- **自動化品質檢查** 和 **信任評分** 能夠幫助使用者更自信地在使用庫存。
- **L0-L3 分類架構** 在大多數情況下應該是可靠的，但某些具體細節可能需要進一步驗證。

### 是否存在邏輯漏洞或偏見？

從技術角度來看，本文沒有明顯的邏輯漏洞。然而，對於特定知識庫和應用場景，還需考慮其他因素，例如是否真的有必要使用 ONNX 進行查詢以及是否有更好的替代方案可以用於語義搜尋。

## 第三層：核心洞察

### 最重要的三個洞見

1. **Vault-for-LLM 是一個四層級的知識管理系統**，能為 LLM 應用程式提供一個簡單且可靠的數據存儲架構。
2. **混合搜索引擎** 能夠有效地結合詞彙與語義向量進行查詢，從而提高效率和準確性。
3. **信任評分** 可以幫助使用者確認知識的可信度。

### 最有價值的啟發

使用 Vault-for-LLM 可以構建一個簡單且可靠的 LLM 應用程式框架。利用混合搜索引擎可以進行高效而准確的語義查詢，通過自動化品質檢查來確保知識庫的安全性與可靠性。

### 如果只能带走一件事，是什麼？

如果只能带走一件事，我會選擇： **使用混合搜索引擎和信任評分功能** 來構建一個可靠的 LLM 應用程式。

## 第四層：系統整合建議

### 如何將此項目融入現有的系統？

1. **模組化設計** ：可以將 Vault-for-LLM 組織為一個模組或插件，供其他應用程式使用。
2. **設定規則** ：可定義一些基本的預設設定和配置參數（例如嵌入式提供商）來簡化初始化過程。
3. **SOP指導** ：可以建立一項標準操作程序(SOP)，以提高團隊在使用 Vault-for-LLM 時的工作效率。

### 具體建議

1. 開發一個模組，涵蓋所有必要的功能和配置設定。該模組可以包括初始化、存儲、讀取、編譯等基本功能。
2. 針對特定需求進行調整，例如添加對某些專有技術或數據庫的支持。
3. 利用提供的 CLI 命令來管理知識庫的生命周期，例如編譯資料、查詢知識庫內容和執行品質檢查。

---

## 💾 元資料

- **研究時間**：2026-04-20 12:33:43
- **來源 URL**：https://github.com/zycaskevin/Vault-for-LLM
- **處理模型**：qwen2.5:3b (本地 Ollama)

---

*由 EnhancedStudy 技能自動產生*
