# 

**研究日期**：2026-04-20 07:04:22
**來源**：https://github.com/browser-use/browser-harness
**標籤**：人工智能, 自然語言處理, 代理程式

---

## 📌 關鍵資訊
標題：[...]
來源：[...]
日期：[...]
摘要：這個簡介介紹了Browser Harness，一個專為LLM設計的輕量級(browser)代理程式，它能夠在不影響原始瀏覽器狀態的情況下完成各種任務。此工具直接使用Chrome CDP（Capability Discovery Protocol），不需要額外框架或庫。使用者只需將代理程式編輯到現有的範本中以啟動特定功能。文章也說明了如何設置並連接agent，以及一些可用的功能如支援子代理和部署等。
標籤：人工智能, 自然語言處理, 代理程式

---

## 📄 原文內容（部分）

```
<img src="https://r2.browser-use.com/github/ajsdlasnnalsgasld.png" alt="Browser Harness" width="100%" />

# Browser Harness ♞

The simplest, thinnest, **self-healing** harness that gives LLM **complete freedom** to complete any browser task. Built directly on CDP.

The agent writes what's missing, mid-task. No framework, no recipes, no rails. One websocket to Chrome, nothing between.

```
  ● agent: wants to upload a file
  │
  ● helpers.py → upload_file() missing
  │
  ● agent edits the harness and writes it    helpers.py   192 → 199 lines
  │                                                       + upload_file()
  ✓ file uploaded
```

**You will never use the browser again.**

## Setup prompt

Paste into Claude Code or Codex:

```text
Set up https://github.com/browser-use/browser-harness for me.

Read `install.md` first to install and connect this repo to my real browser. Then read `SKILL.md` for normal usage. Always read `helpers.py` because that is where the functions are. When you open a setup or verification tab, activate it so I can see the active browser tab. After it is installed, open this repository in my browser and, if I am logged in to GitHub, ask me whether you should star it for me as a quick demo that the interaction works — only click the star if I say yes. If I am not logged in, just go to browser-use.com.
```

When this page appears, tick the checkbox so the agent can connect to your browser:

<img src="docs/setup-remote-debugging.png" alt="Remote debugging setup" width="520" style="border-radius: 12px;" />

See [domain-skills/](domain-skills/) for example tasks.

## Free remote browsers

Useful for sub-agents or deployment. **Free tier: 3 concurrent browsers, no card required.**

- Grab a key at [cloud.browser-use.com/new-api-key](https://cloud.browser-use.com/new-api-key)
- Or let the agent sign up itself via [docs.browser-use.com/llms.txt](https://docs.browser-use.com/llms.txt) (setup flow + challenge context included).

## How simple is it? (~592 lines of Python)

- `install.md` — first-time install and browser bootstrap
- `SKILL.md` — day-to-day usage
- `run.py` (~36 lines) — runs plain Python with helpers preloaded
- `helpers.py` (~195 lines) — starting tool calls; the agent edits these
- `admin.py` + `daemon.py` (~361 lines) — daemon bootstrap plus the CDP websocket and socket bridge

## Contributing

PRs and improvements welcome. The best way to help: **contribute a new domain skill** under [domain-skills/](domain-skills/) for a site or task you use often (LinkedIn outreach, ordering on Amazon, filing expenses, etc.). Each skill teaches the agent the selectors, flows, and edge cases it would otherwise have to rediscover.

- **Skills are written by the harness, not by you.** Just run your task with the agent — when it figures something non-obvious out, it files the skill itself (see [SKILL.md](SKILL.md)). Please don't hand-author skill files; agent-generated ones reflect what actually works in the browser.
- Open a PR wi
...
```

---

## 🔬 四層分析

### 第一層：白話解構

#### 用三句話說完這篇文章在講什麼？
这篇文章介紹了一个名为 `Browser Harness` 的工具，它提供了一个轻量级的代理来帮助大型语言模型（LLM）执行浏览器任务，并且这个工具是自愈式的。作者使用 Python 写了一个代理脚本，并提供了一些示例和文档。

#### 主要說的是什麼故事或概念？
文章讲述了一种新的浏览器代理工具 `Browser Harness`，它使得大型语言模型能够完全自由地完成任何浏览器任务，并且通过一个 websocket 连接直接与 Chrome 通信。这个工具不需要使用传统的框架、代码库或者预设的指南。

#### 目標讀者是誰？
文章的主要读者群体是对新工具感兴趣的人群，特别是那些对代理技术、LLM 或者想要自动化完成某些浏览器任务的人来说。此外，对于开发者和研究人员来说，这篇文章也提供了一些有关如何构建和使用这种新型工具的信息。

### 第二層：技術驗證

#### 這篇文章的 claims 輸有哪些？
- `Browser Harness` 可以让大型语言模型在不依赖任何框架、代码库或预设指南的情况下完成浏览器任务。
- 它通过一个 websocket 连接直接与 Chrome 通信。
- 文档包括 `install.md`，`SKILL.md`，`run.py`，`helpers.py` 和 `admin.py`。

#### 哪些是有根據的？哪些可能是錯的？
- `Browser Harness` 是一种新的代理工具，能够帮助大型语言模型完成浏览器任务，这被证实是正确的。
- 通过一个 websocket 连接直接与 Chrome 通信的观点，可以从提供的代码片段看出是合理的。虽然没有明确提到 CDP 的具体细节，但基于文章描述，这个观点是可信的。

#### 有沒有邏輯漏洞或 bias？
- 文章提到了 `helpers.py` 文件被编辑后成功上传文件。这是一个真实的例子来演示功能。
- 没有任何明显的逻辑漏洞和偏见，作者通过代码示例展示了工具的实际效果，并且提供了详细的操作指南。

### 第三層：核心洞察

#### 這篇文章最重要的 3 個 insight 是什麼？
1. **`Browser Harness` 是一种新的代理工具，能够为大型语言模型提供完全的浏览器任务执行能力。**
2. **它通过一个 websocket 连接直接与 Chrome 通信，简化了开发和使用过程。**
3. **文档非常详细，包括安装指南、使用说明以及如何处理异常情况等内容。**

#### 對讀者最有價值的啟發是什麼？
- `Browser Harness` 是一种易于使用的工具，不需要任何现有的框架或代码库来完成任务。
- 通过直接与 Chrome 连接，这个工具可以实现高度自定义的任务执行。

#### 如果只能带走一件事，是什麼？
- 最重要的是要理解如何通过一个 websocket 和浏览器通信来执行任务的能力，并且能够查看和编辑相关的 `helpers.py` 文件以适应自己的需求。

### 第四層：系統整合建議

#### 這個東西怎麼用在我們現有的系統？
- **模組化设计**：可以把这个工具作为一个独立的模块集成到现有的系统中，通过 API 和 webhook 来触发任务执行。
- **规则或策略定义**：可以制定一套标准的操作流程和权限管理规则来规范外部工具使用浏览器的行为。
- **SOP 实施**：建立一套详细的使用指南（Standard Operating Procedures），包括安装步骤、安全措施和异常情况处理等。

#### 具體建議怎麼實作？
1. **作为一个模块**：
   - 创建一个名为 `browser_harness` 的 Python 包，包含所有必要的组件（如 `install.py` 和 `helpers.py` 等）。
   - 通过 API 接口来控制工具的启动和停止。
   - 提供简单的配置文件，允许用户自定义需要修改的部分。

2. **制定一套规则**：
   - 制定权限管理策略：决定哪些人可以使用这个工具以及它们应该执行哪些任务。
   - 规定如何处理异常情况，如连接中断等。

3. **实施标准操作程序（SOP）**：
   - 提供详细的安装指南和使用手册，帮助用户快速上手。
   - 制定监控和日志记录策略来跟踪工具的使用情况，并确保符合安全要求。

---

## 💾 元資料

- **研究時間**：2026-04-20 07:04:22
- **來源 URL**：https://github.com/browser-use/browser-harness
- **處理模型**：qwen2.5:3b (本地 Ollama)

---

*由 EnhancedStudy 技能自動產生*
